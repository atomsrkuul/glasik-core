[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "glasik-core"
requires-python = ">=3.8"
version = "4.4.0"

[tool.maturin]
features = ["python"]
bindings = "pyo3"
