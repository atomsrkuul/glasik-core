[package]
name = "gn-native"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2", features = ["napi6"] }
napi-derive = "2"
glasik-core = { path = ".." }
libdeflater = "1.25"
rayon = "1.10"

[profile.release]
lto = true
codegen-units = 1
